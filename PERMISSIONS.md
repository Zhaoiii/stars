# 权限系统设计文档

## 用户角色定义

系统现在支持三种用户角色：

1. **管理员 (ADMIN)**

   - 拥有系统最高权限
   - 可以管理所有用户、团队和系统配置
   - 可以访问所有功能模块

2. **管理教师 (MANAGER_TEACHER)**

   - 可以管理团队和学生
   - 可以创建、编辑、删除团队
   - 可以管理学生信息
   - 不能管理其他用户

3. **普通教师 (TEACHER)**
   - 只能管理学生信息
   - 不能管理团队和其他用户
   - 基础的教学功能权限

## 权限矩阵

| 功能模块 | 管理员 | 管理教师 | 普通教师 |
| -------- | ------ | -------- | -------- |
| 用户管理 | ✅     | ❌       | ❌       |
| 团队管理 | ✅     | ✅       | ❌       |
| 学生管理 | ✅     | ✅       | ✅       |
| 评估工具 | ✅     | ❌       | ❌       |
| 模块配置 | ✅     | ❌       | ❌       |
| 领域配置 | ✅     | ❌       | ❌       |
| 阶段配置 | ✅     | ❌       | ❌       |
| 项目配置 | ✅     | ❌       | ❌       |

## 团队管理权限

- **管理员**: 可以管理所有团队
- **管理教师**: 可以管理团队，团队内的管理教师自动成为团队管理者（可以有多个）
- **普通教师**: 只能查看团队信息，不能进行管理操作

### 团队管理者机制

- 团队不再设置固定的负责人（leader）
- 团队内的所有管理教师自动成为团队管理者
- 一个团队可以有多个管理教师
- 团队列表会显示所有管理教师的姓名

## 用户创建规则

- 管理员创建用户时必须选择角色
- 创建教师（管理教师或普通教师）时必须选择所属团队
- 管理员不需要选择团队

## 技术实现

### 后端权限控制

1. **中间件权限检查**

   - `requireAdmin`: 仅管理员
   - `requireAdminOrManagerTeacher`: 管理员或管理教师
   - `requireTeacherOrAdmin`: 所有教师角色和管理员

2. **路由权限配置**
   - 团队管理路由使用 `requireAdminOrManagerTeacher`
   - 用户管理路由使用 `requireAdmin`
   - 学生管理路由使用 `requireTeacherOrAdmin`

### 前端权限控制

1. **路由权限检查**

   - 根据用户角色动态生成菜单
   - 页面访问时进行权限验证

2. **组件级权限控制**
   - 根据用户角色显示/隐藏操作按钮
   - 表单字段根据角色动态启用/禁用

## 数据库变更

由于您选择直接删库重新初始化，以下变更将在数据库重建时生效：

1. **用户角色枚举更新**

   - 添加 `manager_teacher` 角色
   - 保持向后兼容性

2. **团队关联**
   - 用户表添加 `teamId` 字段
   - 建立用户与团队的多对一关系

## 使用说明

1. **创建用户时**：

   - 选择"管理教师"或"普通教师"时必须选择团队
   - 选择"管理员"时不需要选择团队

2. **权限分配**：

   - 管理员拥有所有权限
   - 管理教师可以管理团队和学生
   - 普通教师只能管理学生

3. **团队管理**：
   - 管理员和管理教师可以创建、编辑、删除团队
   - 普通教师只能查看团队信息

## 测试

运行权限测试脚本：

```bash
cd backend
npm run ts-node src/scripts/testPermissions.ts
```

这将验证所有权限组合是否正确工作。
